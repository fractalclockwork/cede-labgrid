# Pin versions for reproducible images (see Dockerfiles / image labels).

BASE_IMAGE_DEBIAN=debian:bookworm-slim

# Raspberry Pi Pico SDK (git tag)
PICO_SDK_VERSION=2.1.1

# Arduino CLI (see https://github.com/arduino/arduino-cli/releases)
ARDUINO_CLI_VERSION=1.0.4

# Python base for orchestration-dev
PYTHON_MINOR=3.12
